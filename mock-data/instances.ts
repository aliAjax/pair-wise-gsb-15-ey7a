import type {Instance} from '../src/types';
import {domains,users} from './catalog';
import {workflows} from './workflows';
const clone=<T,>(x:T):T=>JSON.parse(JSON.stringify(x));
export const instances:Instance[]=Array.from({length:80},(_,i)=>{const status:Instance['status']=i<12?'abnormal':i<22?'timeout':i<50?'running':'completed'; const wf=workflows[i%12]; const currentNode=i%3===0?'直属主管审批':'金额判断'; const seeded=i===4; return {id:`INS-2026-${String(i+1).padStart(4,'0')}`,workflowId:`wf-${i%12+1}`,applicant:users[i%8],domain:domains[i%5],currentNode,status,submittedAt:`2026-07-${String(10-i%9).padStart(2,'0')} ${String(8+i%10).padStart(2,'0')}:10`,duration:status==='timeout'?`${28+i}h`:`${i%9+1}h ${i%6*10}m`,risk:i<22?'high':i<45?'medium':'low',snapshot:{nodes:clone(wf.nodes),edges:clone(wf.edges)},activeRetry:null,attempts:seeded?[{seq:1,node:currentNode,role:'系统管理员',operator:'林秋',startedAt:'2026-07-10 09:40',finishedAt:'2026-07-10 09:41',result:'failed'}]:[],timeline:[{title:'提交申请',time:'09:10',status:'completed'},{title:'直属主管审批',time:'10:24',status:i%3===0?'current':'completed'},{title:'金额判断',time:'11:05',status:i%3!==0?'current':'pending'},...(seeded?[{title:'第 1 次重试失败 · 保留原异常',time:'11:40',status:'failed'}]:[])]};});
